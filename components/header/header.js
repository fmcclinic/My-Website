// header.js
const header = document.getElementById('header');

// Add event listeners or other functionalities to the header
header.addEventListener('click', () => {
  console.log('Header clicked!');
});